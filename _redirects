# Minimal safe Netlify redirects — debug-friendly
# Only explicit page normalizations. NO HTTP/HTTPS or www rules here.
# Use this temporarily to confirm Netlify/Domain settings and remove loops.

/index.html / 301
/home / 301

# Note: restore other redirects from `_redirects.bak` after verifying Netlify
# automatic domain/SSL settings. Do NOT add force (!) rules while debugging.
